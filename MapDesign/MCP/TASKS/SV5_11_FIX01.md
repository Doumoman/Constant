---
mcp_patch:
  format: single_task_v1
  task_id: SV5_11_FIX01
  task_file: TASKS/SV5_11_FIX01.md
  requires_current_task: NONE
  requires_completed_task: SV5_11_HUB_SHELL
  requires_result:
    path: REPORTS/SV5_11_HUB_SHELL_RESULT.md
    status: PASS
    sha256: c384e61a259508e396b370742b677b2751ef6f48210193b74c6bf6f3f0480fdd
  requires_installed_task:
    path: TASKS/SV5_11_HUB_SHELL.md
    sha256: 491f9b9df0b498bc6351efe1c9ff60415fa2bdb91b7e559cc20af558e5f8b97b
  sets_current_task: SV5_11_FIX01
---

# SV5_11_FIX01 — replace synthetic Hub lines with actual-cell connections

TASK: SV5_11_FIX01
EXPECTED RESULT: MapDesign/MCP/REPORTS/SV5_11_FIX01_RESULT.md
NEXT: SV5_12_TREE_GRAB — LOCKED / DO NOT START

## Registration and binding

Run only after the explicitly approved `SV5_11_FIX01_REG` helper has added the
one LOCKED row to Status/Master and staged this file as the sole inbox candidate.
Use the normal native Phase A byte-for-byte install/open/archive process. The
registration itself is not Apply, implementation, Finalize, or PASS.

Before implementation, run the registration bridge's `--verify-task-inputs`.
Bind base commit `0ab603362b93047a45c6f30bac8e28d57ac57f93`, predecessor commit,
Result/Task/Archive/Binding hashes, package manifest, installed/archive Task hash,
and actual before-state in `GENERATED/SV5_11_FIX01/BINDING.json`. Do not force HEAD,
rewrite history, or include unrelated dirty changes.

## Required reads

- Current MCP 00/01/05/07/08/APPLY rules, Status, Master, and this installed Task.
- `MCP/INPUTS/SV5_11_FIX01_REG/{README,REGISTRATION,CONTRACT}.md`,
  `HUB_CONNECTION_PROFILE.json`, `SOURCE_LOCK.json`, and the independent checker.
- Predecessor Task, Archive, Result, Binding, focused XML, checker, default/repeat
  Hub exports, and current Hub runtime/tests listed in `SOURCE_LOCK.json`.
- Current loop/sidepath occupancy, endpoint, movement, physical product, protected
  cell, and export implementations needed to validate actual cells.

## Write allowlist

- `Assets/_Game/Map/Runtime/WorldGeneration/SectorPlanning/Sv5HubShell.cs`
- `Assets/_Game/Map/Runtime/WorldGeneration/SectorPlanning/Sv5HubShellExport.cs`
- `Assets/_Game/Map/Runtime/WorldGeneration/SectorPlanning/Sv5SpaceGraphPlan.cs`
- `Assets/_Game/Map/Runtime/WorldGeneration/SectorPlanning/Sv5SpaceGraphPlanner.cs`
- `Assets/_Game/Map/Runtime/WorldGeneration/SectorPlanning/Sv5SpaceGraphExport.cs`
  only if minimally required for post-Hub occupancy/product evidence
- new `Sv5HubConnectionRouter.cs` and matching `.meta` if separation is useful
- `Assets/_Game/Tests/EditMode/Map/SV5/Sv5HubShellTests.cs`
- new `Sv5HubShellFix01Tests.cs` and matching `.meta`
- `MapDesign/MCP/SV5/19_HUB_SHELL_FIX01_V5.md`
- `MapDesign/MCP/GENERATED/SV5_11_FIX01/**`
- this registration input folder, registration record/receipt, installed Task,
  Archive, matching Result, Master one-row registration, and normal Status lifecycle

Do not modify predecessor generated evidence, Result, Task, Archive, Binding,
core/loop/sidepath evidence, scene, Player, packages, project settings, or any
retired/legacy export. Do not restore, checkout, reset, clean, or delete unrelated
files. If an allowlisted source already has an unrelated uncommitted change, stop.

## Required implementation

1. Reproduce and preserve a failing test showing the old vertical-then-horizontal
   line crossing blocked/protected cells while its booleans still report clear.
2. Replace claim-only `Path()` acceptance with bounded deterministic routing over
   actual 1×1 occupancy and the existing movement rules. Use sparse local overlays.
3. Apply accepted connection centerline/clearance/support changes to final Hub
   occupancy with explicit ownership. The external anchor is the only read-only
   foreign contact; it cannot be modified.
4. Compute protected, Type0, and progression results from real source sets and
   connection body cells. Remove hardcoded false properties and reject collision
   or bypass candidates.
5. Preserve six sockets and accept four to six distinct external Rooms and
   SpaceGroups. Store at least one valid direction per connection; reverse traversal
   is optional and must not be fabricated.
6. Rebuild post-Hub movement/topology and the nine-state × six-order physical
   product from final occupancy once per profile. Keep projection proofs successful.
7. Export every C05 artifact from the tested default/repeat plans. Bind upstream
   constraint source paths/hashes so the independent checker recomputes geometry.
8. Keep per-candidate world copy/BFS at zero, obey local caps, and export timings,
   route attempts, rejection histogram, and global product run count.
9. Run targeted tests, then the independent checker, then one full SV5 regression
   only after the first two pass. Meet all C06 counts with 0 failure/skip.
10. Run `REGISTER.py --post-readonly`, write a matching PASS Result, finalize only
    on PASS, and make one atomic Task-owned commit including registration. Do not
    start SV5_12 or push.

## Stop conditions

Stop as BLOCKED before mutation if package, Git object, state, inbox, collision,
or unrelated-overlap checks fail. During implementation, normal code/test failures
are iterative work, not a reason to finalize or weaken the contract. If the bounded
model cannot yield four actual connections, report the failing geometry and counts;
do not restore the synthetic line behavior.

After the atomic commit, create `SV5_11_FIX01_REVIEW.zip` from exact commit blobs.
Include `_REVIEW_MANIFEST.json`, changed source/tests, Task/Archive/Result, final
Status/Master, Binding, registration receipt/record, default/repeat exports, final
XML, independent audit, and the checker. Report commit/title, test counts/duration,
connection counts, product counts, performance, ZIP SHA, and stop.

