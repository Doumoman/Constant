# MCP PATCH — MAP01_13 CONTENT VERSION HASH v1.0

MAP01_12 PASS (Registry `47`, targeted `562/562`, full `582/582`, compile/warning `0/0`)를 gate로 49 static semantic records를 canonicalize하여 file/row order-independent SHA-256를 계산한다.

적용 전: Current Task `NONE`, MAP00_01~10/MAP01_01~12 `COMPLETE`, MAP01_13 이후 `LOCKED`, 205 tasks.

폴더를 `MapDesign/MCP_INBOX/` 바로 아래에 넣고 `.APPLIED` 없는 패치는 이것 하나만 둔 뒤 `MapDesign/MCP/APPLY_PATCH_AND_RUN_CURRENT_TASK.md를 수행해.`

완료 후 `MapDesign/MCP/REPORTS/MAP01_13_IMPLEMENT_CONTENT_VERSION_HASH_RESULT.md`를 가져온다. MAP01_14는 자동 시작하지 않는다.
