# MAP05_01 — Build Mandatory Terminals

MAP04 exit approval 뒤 MAP05의 첫 Task만 여는 patch package다. Apply는 Master, Status, 새 Task 문서만 설치하고 Assets는 변경하지 않는다.

실행 시 StartAnchor 1개와 six required site entry exterior 6개를 exact immutable 7-terminal set으로 만든다. route mask, connector tree, routing, Type2/3 gateway, graph, validator, overlay는 시작하지 않는다.

기준선은 MAP04 exit approved, progress test scene ready, Assets meta `3152`, Authoring CSV/meta `50/50`이다.
