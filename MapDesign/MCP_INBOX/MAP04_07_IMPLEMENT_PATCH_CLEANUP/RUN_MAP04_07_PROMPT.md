# RUN MAP04_07

MCP control 문서 → Master/Status → 현재 Task → MAP04_06 Result 순으로 읽고 `TASKS/MAP04_07_IMPLEMENT_PATCH_CLEANUP.md`를 exact 실행하라.

허용된 신규 C#/meta/Result만 만들고 existing Assets/CSV/asmdef/Scene/Prefab를 수정하지 마. actual focused `>=120`, required regression `263/263`, actual total `>=383`, failed/skipped `0/0`; targeted/full은 discovery-only `>=4785/>=4853`; compile/Console/warning `0/0/0`; final meta `3125`; exact Assets changes `14`를 확인하라.

PASS일 때 MAP04_07만 COMPLETE/Current NONE으로 finalize하고 MAP04_08은 LOCKED로 유지하라. Result는 Task의 compact template만 사용하라.
