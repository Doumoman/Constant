# MAP04_06 — Implement Intrusion Placement

MAP04_05 PASS 상태에서 MAP04의 여섯 번째 Task만 여는 patch package다. Patch apply는 Master, Status, 새 Task 문서만 설치하고 Assets를 변경하지 않는다.

내부 `RUN_MAP04_06_PROMPT.md`로 실행한다. MAP04_05의 viable grown publication과 continued same-attempt `RNG_BIOME_PATCH`를 입력으로, Root/Mill Intrusion count를 count-first 추첨하고 active pair rule이 `BOUND_TUNNEL/TUNNEL_INTRUSION`을 허용하는 cardinal biome boundary에서만 one-cell Intrusion patch를 atomic 배치한다.

protected Core/Satellite seed·Core binding·P01 reservation은 보존하고, donor patch가 rule minimum 아래로 내려가거나 분리되는 sector는 선택하지 않는다. general one-cell patch는 여전히 금지하며 explicit `AllowSingleSector=true` Intrusion만 허용한다. cleanup, export, final validator, overlay, `PASS_BIOME` root adapter는 MAP04_07 이후 범위다. 기준선은 MAP04_05 actual focused `164/164`, actual required `642/642`, Assets meta `3110`, Authoring CSV/meta `50/50`이다.
