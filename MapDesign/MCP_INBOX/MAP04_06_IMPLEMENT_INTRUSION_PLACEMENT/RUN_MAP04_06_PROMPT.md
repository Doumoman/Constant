# RUN MAP04_06

`MapDesign/MCP/00_MCP_ENTRYPOINT.md`부터 locked/work/CSV/Unity/change/patch/finalize rules, Master, Status, 현재 `TASKS/MAP04_06_IMPLEMENT_INTRUSION_PLACEMENT.md`, MAP04_05 PASS Result를 순서대로 읽어라.

Task의 exact READ/WRITE ALLOWLIST를 준수해 approved Runtime/Test `Generation` 폴더에 production C# 7개, `IntrusionPlacerTests.cs` 1개와 matching meta 8개만 추가하라. existing production/tests/meta/asmdef/CSV/Scene/Prefab를 수정하지 마.

successful MAP04_05 result와 continued same-attempt `RNG_BIOME_PATCH`를 입력으로 받아 `PATCH_MILL_INTRUSION`, `PATCH_ROOT_INTRUSION` count를 먼저 inclusive draw하라. actual viable fixture의 before DrawCount `1907`, count vector `MILL/ROOT=1/2`, count raw draws `2`를 증명하라. candidate는 typed pair rule의 `BOUND_TUNNEL/TUNNEL_INTRUSION` 허용, cardinal shared edge, non-edge, unreserved, non-seed/non-binding, donor minimum·connectivity, same-rule intrusion distance를 모두 통과해야 한다. sorted SectorIndex candidate에 exact one `NextInt(count)`를 사용해 one-cell Intrusion patch로 transfer하고 failure은 atomic rollback하라.

actual focused `>=150`, MultiSeedBiomeGrower `164/164`, SatelliteSeedPlacer `141/141`, BiomePatchModels `107/107`, DeterministicRngStream `103/103`, required regressions `515/515`, actual required total `>=665`, failed/skipped `0/0`을 실행하라. targeted/full은 각각 discovery arithmetic/resource `>=4659 / >=4727`로만 확인하고 실행 PASS로 표기하지 마. compile/Console/relevant warning `0/0/0`, Authoring CSV/meta `50/50`, final Assets meta `3118`, duplicate GUID `0`, exact Assets changes `16`, existing Assets modification `0`을 확인하라.

Core/Satellite 재성장, Intrusion multi-cell growth, arbitrary pair, chain expansion, cleanup, checkerboard/neck 후처리, generated CSV/export, final validator, overlay, `PASS_BIOME` adapter/root/retry loop를 구현하지 마. 전부 PASS일 때 MAP04_06 COMPLETE/Current Task NONE으로만 finalize하고 `MAP04_07_IMPLEMENT_PATCH_CLEANUP`은 LOCKED로 유지하라.
