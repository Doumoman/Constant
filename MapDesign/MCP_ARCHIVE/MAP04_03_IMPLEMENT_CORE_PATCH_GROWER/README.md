# MAP04_03 — Implement Core Patch Grower

MAP04_02 PASS 상태에서 MAP04의 세 번째 Task만 여는 patch package다. Patch apply는 Master, Status, 새 Task 문서만 설치하고 Assets를 변경하지 않는다.

내부 `RUN_MAP04_03_PROMPT.md`로 실행한다. MAP04_02의 exact four CorePatch를 source footprint의 cardinal buffer와 rule minimum까지 다른 P01 reservation 침범 없이 atomic 성장시킨다. starter는 `4 -> 20 assigned`, `165 -> 149 unassigned`, mandatory `+16`, supplemental `0`, RNG `0`이다.

MAP03 capacity witness는 복사하지 않고 독립 재계산한다. Satellite/Intrusion, remaining-world cost/noise/RNG, cleanup, export, validator, overlay, `PASS_BIOME` 실행은 MAP04_04 이후 범위이므로 시작하지 않는다. 기준선은 MAP04_02 focused `121/121`, actual required `643/643`, Assets meta `3086`, Authoring CSV/meta `50/50`이다.
