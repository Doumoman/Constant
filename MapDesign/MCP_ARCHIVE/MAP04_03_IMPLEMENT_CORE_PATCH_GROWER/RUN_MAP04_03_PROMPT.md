# RUN MAP04_03

`MapDesign/MCP/00_MCP_ENTRYPOINT.md`부터 locked/work/CSV/Unity/change/patch/finalize rules, Master, Status, 현재 `TASKS/MAP04_03_IMPLEMENT_CORE_PATCH_GROWER.md`, MAP04_02 PASS Result를 순서대로 읽어라.

Task의 exact READ/WRITE ALLOWLIST를 준수해 기존 approved Runtime/Test `Generation` 폴더에 production C# 6개, `CorePatchGrowerTests.cs` 1개와 matching meta 7개만 추가하라. existing production/tests/meta/asmdef/CSV/Scene/Prefab를 수정하지 마.

MAP04_02 publication을 입력으로 four CorePatch의 source footprint 전체에서 cardinal mandatory buffer를 계산하고 target `max(buffer count, rule minimum)`까지 성장시켜라. 다른 P01 reservation은 모두 hard blocker다. starter는 per-patch `5/5/5/5`, `20 assigned / 149 unassigned`, mandatory `+16`, supplemental `0`, reservation intrusion/overlap/RNG `0/0/0`이다.

actual focused `>=120`, CorePatchSeedInitializer `121/121`, BiomePatchModels `107/107`, CoreCapacityFloodChecker `215/215`, actual required total `>=563`, failed/skipped `0/0`을 실행하라. targeted/full은 각각 discovery `>=4197 / >=4266`로 확인하되 실행 PASS로 표기하지 마. compile/Console/relevant warning `0/0/0`, Authoring CSV/meta `50/50`, final Assets meta `3093`, duplicate GUID `0`, exact Assets changes `14`, existing Assets modification `0`을 확인하라.

MAP03 capacity witness 복사/dependency, Satellite/Intrusion, full-map altitude/noise/cost/RNG, cleanup, `PASS_BIOME`, generated CSV, validator, overlay를 구현하지 마. 전부 PASS일 때 MAP04_03 COMPLETE/Current Task NONE으로만 finalize하고 `MAP04_04_IMPLEMENT_SATELLITE_SEED_PLACER`는 LOCKED로 유지하라.
