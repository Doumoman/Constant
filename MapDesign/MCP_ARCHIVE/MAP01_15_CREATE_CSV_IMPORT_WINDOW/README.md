# MCP PATCH — MAP01_15 CSV IMPORT WINDOW v1.0

MAP01_14 PASS (atomic/report `55`, targeted `671/671`, full `691/691`, compile/warning `0/0`)를 gate로 fixed Authoring CSV 50개 전체 재임포트, 파일별 row/error/raw hash, global ContentVersionHash, source/FK navigation을 제공하는 EditorWindow를 만든다.

적용 전: Current Task `NONE`, MAP00_01~10/MAP01_01~14 `COMPLETE`, MAP01_15 이후 `LOCKED`, 205 tasks.

폴더를 `MapDesign/MCP_INBOX/` 바로 아래에 넣고 `.APPLIED` 없는 패치는 이것 하나만 둔 뒤 `MapDesign/MCP/APPLY_PATCH_AND_RUN_CURRENT_TASK.md를 수행해.`

완료 후 `MapDesign/MCP/REPORTS/MAP01_15_CREATE_CSV_IMPORT_WINDOW_RESULT.md`를 가져온다. MAP01_16은 자동 시작하지 않는다.
