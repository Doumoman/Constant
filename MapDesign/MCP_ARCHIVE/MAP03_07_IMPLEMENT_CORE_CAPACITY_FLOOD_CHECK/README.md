# MAP03_07 — Implement Core Capacity Flood Check

MAP03_06 PASS 상태에서 MAP03의 일곱 번째 Task 하나만 여는 patch package다. Patch apply는 Master, Status, 새 Task 문서만 설치하고 Assets를 변경하지 않는다.

내부 `RUN_MAP03_07_PROMPT.md`로 실행한다. 기존 approved `Generation` 폴더에 Runtime production C# 8개와 focused EditMode test 1개만 추가한다. MAP03_06의 selected Forge/Core 3개 footprint를 multi-source seed로 삼아 cardinal buffer와 Core 최소 크기를 만족하는 connected capacity를 검사하고, canonical order의 pairwise-disjoint witness를 만든다.

이 Task는 `CoreCapacityApproval`까지만 만든다. Village, `CoreBiomeSeed`, final `SiteReservation`/snapshot/ID publication, biome painting은 후속 Task로 남긴다. 기준선은 focused `248/248`, targeted `2790/2790`, full EditMode `2830/2830`, Assets meta `3036`, Authoring CSV/meta `50/50`이다.
