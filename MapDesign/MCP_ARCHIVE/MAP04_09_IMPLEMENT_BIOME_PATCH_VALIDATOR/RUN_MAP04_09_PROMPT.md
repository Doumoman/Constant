# RUN MAP04_09

MCP control 문서 → Master/Status → 현재 Task → MAP04_08 Result 순으로 읽고 `TASKS/MAP04_09_IMPLEMENT_BIOME_PATCH_VALIDATOR.md`를 exact 실행하라.

허용된 신규 C#/meta/Result만 만들고 existing Assets/CSV를 수정하지 마. focused `>=150`, regressions `248/248`, actual `>=398`, failed/skipped `0/0`; discovery-only targeted/full `>=5083/>=5152`; compile/Console/warning `0/0/0`; final meta `3140`; exact Assets changes `16`을 확인하라.

PASS일 때 MAP04_09만 COMPLETE/Current NONE으로 finalize하고 MAP04_10은 LOCKED로 유지하라. Result는 compact template만 사용하라.
