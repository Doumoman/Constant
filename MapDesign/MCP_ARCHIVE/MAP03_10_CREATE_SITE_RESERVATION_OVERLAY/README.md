# MAP03_10 — Create Site Reservation Overlay

MAP03_09 PASS/finalize 상태에서 MAP03의 열 번째 Task 하나만 여는 patch package다. Patch apply는 Master, Status, 새 Task 문서만 설치하고 Assets를 변경하지 않는다.

내부 `RUN_MAP03_10_PROMPT.md`로 실행한다. 기존 approved Runtime `Diagnostics`, Editor `Preview`, Runtime test `Generation`, Editor test `Preview` 폴더에 production C# `4+1`, focused EditMode test C# `1+1`, matching meta `7`만 추가한다.

MAP03_09의 exact final publication과 MAP03_06/07/08/09 completed diagnostics를 immutable 169-cell overlay snapshot으로 투영한다. Game View와 Scene View는 같은 runtime GUI renderer를 사용하며 seven source-colored footprints, final local coordinates, six entry arrows, four minimum Core witness regions/20 sectors, candidate rejection/final-gate/soft-cost diagnostics를 표시한다.

이 Task는 display-only다. generation/pass/root/retry/RNG, candidate repair, 100,000-seed batch, biome growth/painting, file I/O를 실행하지 않는다. 기준선은 MAP03_09 focused `268/268`, targeted `3612/3612`, full EditMode `3652/3652`, Assets meta `3063`, Authoring CSV/meta `50/50`이다.
