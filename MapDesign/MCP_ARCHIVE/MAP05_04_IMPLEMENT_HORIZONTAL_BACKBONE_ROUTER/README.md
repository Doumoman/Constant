# MAP05_04 — Implement Horizontal Backbone Router

MAP05_03 connector tree PASS 뒤 MAP05의 네 번째 Task만 여는 patch package다. Apply는 Master, Status, 새 Task 문서만 설치하고 Assets는 변경하지 않는다.

실행 시 MAP05_03 connector tree edge마다 horizontal-only backbone segment 후보를 찾는다.

출력은 L/R 관통 일반 sector run 후보와 diagnostics뿐이다. Type2/3 vertical gateway, U/D conflict, loops, final graph, generated CSV, validator, overlay는 시작하지 않는다.

기준선은 MAP05_03 PASS, Assets meta `3179`, Authoring CSV/meta `50/50`이다.
