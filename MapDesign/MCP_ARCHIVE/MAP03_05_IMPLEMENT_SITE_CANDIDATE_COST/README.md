# MAP03_05 — Implement Site Candidate Cost

MAP03_04 PASS 상태에서 MAP03의 다섯 번째 Task만 여는 patch package다. Patch apply는 Master, Status, 새 Task 문서만 설치하고 Assets를 변경하지 않는다.

내부 `RUN_MAP03_05_PROMPT.md`로 실행한다. 기존 approved `Generation` 폴더에 Runtime production C# 6개와 focused EditMode test 1개만 추가한다. MAP03_03 성공 placement 하나를 MAP03_04 partial distance policy에 대입하고 altitude, edge, distance deficit, optional future Core capacity forecast, three-Core 4×4 clustering의 immutable integer cost breakdown을 만든다.

이 Task는 점수와 hard signal만 만들며 후보 정렬·RNG·선택·reservation 생성·backtracking·실제 capacity flood를 하지 않는다. 기준선은 targeted `2272/2272`, full EditMode `2312/2312`, Assets meta `3020`, Authoring CSV/meta `50/50`이다.
