# MAP03_01 — Implement Site Reservation Models

MAP02 exit approval 상태에서 MAP03의 첫 번째 Task만 여는 patch package다. Patch apply는 Master, Status, 새 Task 문서만 설치하고 Assets를 변경하지 않는다.

내부 `RUN_MAP03_01_PROMPT.md`로 실행한다. 기존 approved `Generation` 폴더에 immutable site-reservation production C# 8개와 focused EditMode test 1개만 추가한다. typed reservation ID, exact enum/token, final-oriented footprint, entry anchor, `CoreBiomeSeed`, 169-cell sector reservation과 complete snapshot을 구현한다.

후보 열거, transform 적용, distance/cost/backtracking, capacity flood, village selection, `PASS_SITE`, generated CSV는 MAP03_02 이후 범위이므로 시작하지 않는다. 기준선은 targeted `1514/1514`, full EditMode `1554/1554`, Assets meta `2989`, Authoring CSV/meta `50/50`이다.
